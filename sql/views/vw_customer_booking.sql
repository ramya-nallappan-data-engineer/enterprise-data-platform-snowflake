CREATE OR REPLACE VIEW VW_CUSTOMER_BOOKINGS AS

SELECT

c.CUSTOMER_ID,

c.FIRST_NAME,

b.BOOKING_ID,

b.TOTAL_AMOUNT,

h.HOTEL_NAME

FROM CUSTOMER_SILVER c

JOIN BOOKING_SILVER b

ON c.CUSTOMER_ID=b.CUSTOMER_ID

JOIN HOTEL_SILVER h

ON b.HOTEL_ID=h.HOTEL_ID;