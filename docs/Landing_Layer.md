**Purpose**

The Landing Layer stores raw data exactly as received from source systems.

Responsibilities
	Preserve source data
	Enable auditing
	Support replay and recovery
	Store ingestion metadata
	Avoid business transformations
Best Practices
	Append-only loads
	Metadata columns
	Source tracking
	Immutable raw data